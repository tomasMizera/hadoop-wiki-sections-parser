# Reducer

import sys

for section in sys.stdin:

    # Save section to file together with article title and section's text
    f.write( section, title, text )

    # pass output to some other process via standard output
    print( reply )
