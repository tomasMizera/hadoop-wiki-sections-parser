# Reducer

# import inverted index builder

import sys

for section in sys.stdin:

    # 1. update inverted index
    _update_index( section )

    # 2. recalculate statistics

    # 3. pass output to some other process via standard output
    print( reply )
